#Task status management through the dev cycle

```mermaid
flowchart LR
    A((start)) --> |Create| B[To Do]
    B -->|Groom| C[Ready]
    C -->|Assign| D[In Progress]
    D -->|code| E{Review}
    E -->|rework| D
    E -->|ready| F[Ready to Deploy]
    F -->|deploy| G[Ready to test] 
    G --> H{Test}
    H -->|pass| I[Done]
    H -->|fail - assign to dev| D
    I --> J((End))
```
